export interface Author {
	id: string;
	name: string;
	birth: number;
	death: number;
}

export interface AuthorRecord {
	id: string;
	name: string;
	birth: number;
	death: number;
}

export interface IAuthorPayload {
	id: string;
	name: string;
	birth: number;
	death: number;
}
